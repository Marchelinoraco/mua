import { ClientDetailSheet } from './client-detail-sheet'

/** Koordinator dialog fitur Klien (F09) — saat ini hanya Sheet detail (profil + riwayat + catatan). */
export function ClientsDialogs() {
  return <ClientDetailSheet />
}
